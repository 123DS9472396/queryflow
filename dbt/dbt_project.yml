name: 'enterprise_nyc_taxi'
version: '1.0.0'
config-version: 2

profile: 'clickhouse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  enterprise_nyc_taxi:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table
